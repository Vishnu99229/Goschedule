# Local env for Vite (do not commit real keys — use .env.local)

# Supabase project URL (no trailing slash)
VITE_SUPABASE_URL=https://lagzlyizyhagwtnychkt.supabase.co

# Supabase anon/public key (Dashboard → Project Settings → API)
VITE_SUPABASE_ANON_KEY=

# Vapi public key (Vapi Dashboard → API Keys → Public)
VITE_VAPI_PUBLIC_KEY=
